#include <stdio.h>
int main() {
    int num;
      scanf("%d",&num);
      if (num < 0){
        printf("numero negativo\n")
        else if (num >= 0)
        
        printf("numero positivo\n")
        if (num % 2 == 0)
        printf("numero par\n")

        else if (num % 2 != 0)
        printf("numero impar\n");

        return 0;
        
      }
}